# Context Builder service configuration (prefix: CONTEXT_BUILDER_)

# Base URL of the Mock LMS Resource APIs the fetch profiles read.
CONTEXT_BUILDER_LMS_BASE_URL=http://127.0.0.1:8000

# Root log level for the service entrypoint (INFO, DEBUG, WARNING, ...).
CONTEXT_BUILDER_LOG_LEVEL=INFO
