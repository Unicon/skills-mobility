"""Context Builder — deterministically assembles the source-data context bundle
the Orchestrator needs, by running a versioned fetch profile against the Mock
LMS Resource APIs. See docs/3_design/context-builder.md."""
